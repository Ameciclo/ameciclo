{
  "routes": [
    {
      "method": "GET",
      "path": "/contagens",
      "handler": "contagens.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/contagens",
      "handler": "contagens.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/contagens",
      "handler": "contagens.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
