'use strict';

/**
 * clipping-media controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::clipping-media.clipping-media');